# OkamiCode Sprints 1–7 Release Evidence

Date: 2026-07-23  
Platform: macOS arm64  
Branch: `codex/okami-sprints-0-7`

## Automated gates

- `pnpm check`: PASS after rebuilding `better-sqlite3-multiple-ciphers` for Node.
  - 113 test files passed; 5 opt-in files skipped.
  - 712 tests passed; 10 opt-in tests skipped.
  - TypeScript, ESLint, and Prettier passed with zero warnings.
- Runtime conformance: PASS for all eight registered runtimes.
- `pnpm test:e2e`: PASS outside the managed macOS sandbox.
  - 3 Electron tests passed.
  - Node integration remained disabled in the renderer.
  - The preload bridge remained available.
  - No page-level horizontal overflow was detected.

## Bounded live smokes

Each paid runtime received at most one successful exact-string turn. The failed
MiMo attempt used an invalid non-canonical model id and produced no assistant
turn; it was corrected once with the id returned by the local model catalog.

| Runtime path | Result |
| --- | --- |
| Claude subscription | `OKAMI_CLAUDE_SMOKE` |
| ChatGPT through the Claude-compatible gateway | `OKAMI_GATEWAY_SMOKE` |
| Codex app-server | Handshake and redacted rate-limit read passed; zero turns consumed |
| Cursor Agent | `OKAMI_CURSOR_SMOKE` |
| Antigravity | `OKAMI_AGY_SMOKE` |
| Grok | `OKAMI_GROK_SMOKE` |
| MiMo Code | `OKAMI_MIMO_SMOKE` |
| MiniMax Token Plan | `OKAMI_MINIMAX_SMOKE` |
| OpenCode ACP | `OKAMI_OPENCODE_SMOKE` |

## Visual inspection

Screenshots generated by the Electron suite:

- `lane-health-1440.png`: inspected at 1440×900.
- `lane-health-760.png`: inspected at 760×900.

The inspection confirmed:

- no overlap or clipped project/composer controls;
- responsive Code layout with the project list retained at 760 px;
- provider/model identity and context/token states remain visible;
- raised-graphite telemetry contrast is readable;
- the activity rail animates only while running and honors reduced motion.

## Package

- Artifact: `release/OkamiCode-v1.0.0-beta.1-macOS-arm64.dmg`
- Size: 149 MiB
- SHA-256:
  `716e47121e71a7e9330ec07a0554e1681579150de32be4066ac52f6987f3e40c`
- Signing: ad-hoc linker signature only; no trusted Developer ID identity was
  available. The package is not notarized.

